services:
    # Ми робимо нашу *конкретну реалізацію* репозиторію публічною
    App\Booking\Infrastructure\Repository\DoctrineEventRepository:
        autowire: true
        public: true

    # Ми також робимо *аліас* (інтерфейс), за яким ми
    # звертаємось до сервісу, публічним
    App\Booking\Domain\Repository\EventRepositoryInterface:
        public: true
