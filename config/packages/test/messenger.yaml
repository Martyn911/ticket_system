# config/packages/test/messenger.yaml
framework:
    messenger:
        transports:
            # Intercept all messages sent to the 'async' transport
            # and store them in the in-memory transport instead of RabbitMQ
            async: 'in-memory://'
